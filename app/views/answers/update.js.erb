$('.answer-errors').html("<%= j render 'shared/errors', resource: @answer %>");

<% if !@answer.errors.present? %>
  $('.answers').html('<%= j render @question.answers.order(:created_at) %>');
<% end %>